import { Typography } from "@mui/material";
import styled from "styled-components";

// Define a styled component for Open Sans font
export const OpenSansText = styled.p`
  font-family: "Open Sans", sans-serif;
`;

// Define a styled component for Comfortaa font
export const ComfortaaText = styled.p`
  font-family: "Comfortaa", sans-serif;
`;

export const ComfortaaTypography = styled(Typography)`
  font-family: "Comfortaa", sans-serif;
  font-weight: bold;
`;
